[package]
name = "elf"
version = "0.1.0"
edition = "2024"

[dependencies]
alisp = { path = "../alisp" }
ureq = { version = "3", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
